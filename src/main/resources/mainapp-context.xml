<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context.xsd">

    <context:annotation-config/> <!-- говорим spring`у что хотим настраивать бины через аннотации -->
    <context:component-scan base-package="ru.chitushka"/> <!-- указываем пакет с которого начнется сканирование поиск аннотаций -->

    <context:property-placeholder location="database.properties"/>
    <context:property-placeholder location="classpath*:/prop/*.properties"/> <!-- здесь будут искаться property файлы -->


    <bean id="messageSer" class="ru.chitushka.services.message.FactoryMessage" factory-method="getInstanceMessageService"/>
    <bean id="musicSer" class="ru.chitushka.services.music.ClassicalMusicPlayer" init-method="initMethod" destroy-method="destroyMethod" />
    <bean id="dataSource" destroy-method="close" class="org.apache.commons.dbcp.BasicDataSource">
        <property name="driverClassName" value="${jdbc.driverClassName}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

</beans>